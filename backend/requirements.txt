
# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0

# Environment and Configuration
python-dotenv==1.0.0

# Database
pymongo==4.6.0
motor==3.3.2
pydantic==2.5.0
bson==0.5.10

# File Upload and Processing
python-multipart==0.0.6
PyPDF2==3.0.1
pdfplumber==0.10.3
fpdf==1.7.2
reportlab==4.0.7

# HTTP and Networking
httpx==0.25.2
requests==2.31.0

# Authentication and Security
passlib==1.7.4
PyJWT==2.10.1
bcrypt==4.1.1

# Text-to-Speech
gTTS==2.4.0

# Utilities
python-multipart==0.0.6
difflib2==0.0.0

# Optional / OCR and document helpers (used by server.py fallbacks)
# Note: these Python packages are wrappers; OCR requires system binaries (tesseract) and
# PDF->image conversion requires poppler-utils (system package). Install those in the
# runtime image if you intend to use OCR features.
pdfminer.six>=20221105
python-docx>=0.8.11
Pillow>=9.0.0
pdf2image>=1.16.0
pytesseract>=0.3.10

# Optional: External integrations (if available)
--extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations
